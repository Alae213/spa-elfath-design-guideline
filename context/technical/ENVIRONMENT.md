# ENVIRONMENT — spa-elfath-design-guideline

No env required for v0 local dev.

| Key | Purpose | Required | Where |
|-----|---------|----------|-------|
| — | — | — | — |

Future: if Algolia/Map provider added, add `NEXT_PUBLIC_*` publishable key here. Never hardcode secrets. `.env*` gitignored.
